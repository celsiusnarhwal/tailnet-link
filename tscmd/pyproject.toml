[project]
name = "tscmd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "celsius narhwal", email = "hello@celsiusnarhwal.dev" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
tscmd = "tscmd.tscmd:main"

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"
